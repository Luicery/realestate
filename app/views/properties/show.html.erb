<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Document</title>
</head>
<body>
  <div id="main">
    <div class="slideshow">
      <% @property.images.each do |x| %><img class="slide" src="<%= x.image %>"><% end %>
    </div>
    <div id="details">
      <% if @current_user.present? == false %>
        <span id="Watchlist"><h3><%= link_to "Add to Watchlist", watch_path(@property.id), method: "patch",  :id => "WatchlistLook"%></h3></span>
      <% else %>
        <% if (@current_user.watch.properties.find_by id:params[:id]).present? == false %>
        <span id="Watchlist"><h3><%= link_to "Add to Watchlist", watch_path(@property.id), method: "patch",  :id => "WatchlistLook"%></h3></span>
        <% else %>
        <span id="Watchlist"><h3><%= link_to "Remove from Watchlist", watch_path(@property.id), method: "delete",  :id => "WatchlistLook"%></h3></span>
        <% end %>
      <% end %>
      <div id="Width_manipulate">
        <h2><%= @property.title %></h2>
        <h3><%= @property.address %><br>
        <img class="logo" src="https://c7.uihere.com/icons/990/543/695/bed-2bcc00ee5d9ffb499c5f90be3fc68ec0.png"><%= @property.bedrooms %>
        <img class="logo" src="https://www.shareicon.net/data/512x512/2016/10/22/846720_clean_512x512.png"><%= @property.bathrooms %>
        <img class="logo" src="https://upload.wikimedia.org/wikipedia/commons/thumb/f/f0/Car_with_Driver-Silhouette.svg/1221px-Car_with_Driver-Silhouette.svg.png"><%= @property.parking %>
        <img class="logo" src="https://static.thenounproject.com/png/560015-200.png"><%= @property.area %>m²</h3>
      </div>
      <br><hr><br>
    </div>
    <div class="inline content"><%= @property.description %></div>
    <div id="agent" class="inline">
      <h3><%= @agent.name %><br></h3>
      <img class="picture" src="<%= @agent.image %>"><br>
      <%= @agent.company %><br>
      Email: <%= @agent.email %><br>
      Mobile: <%= @agent.phone %><br>
    </div>

  </div>
  <div id="map">

  </div>
  <script>
    function initMap() {
      geocoder = new google.maps.Geocoder();
      var input =  "<%= @property.address %>";
      var address = "<%= @property.address %>";
      geocoder.geocode( { 'address': address}, function(results, status) {
      if (status == 'OK') {
        map.setCenter(results[0].geometry.location);
        var marker = new google.maps.Marker({
            map: map,
            position: results[0].geometry.location
        });
      } else {
        alert('Geocode was not successful for the following reason: ' + status);
      }
    });
      var map = new google.maps.Map(document.getElementById('map'), {zoom: 20, center: {lat: -25.344, lng: 131.036}});
    }
  </script>
  <script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDkebnqIrf98MzWYEb0T2T0jd13MrkQNw4&callback=initMap"
  type="text/javascript"></script>
</body>
</html>
